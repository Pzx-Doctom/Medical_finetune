# 医疗 LoRA 微调实践 - 锁定版本依赖
# Python 3.10+

# 深度学习框架
torch==2.2.1
numpy==1.26.4  # torch 2.2.1 要求 numpy<2

# 模型库（加载 Qwen1.5-0.5B、Tokenizer、Trainer）
transformers==4.39.3

# 参数高效微调（LoraConfig + get_peft_model + PeftModel）
peft==0.9.0

# 数据集处理（load_dataset 下载/缓存/采样）
datasets==2.18.0

# 训练加速（Trainer 底层依赖，管理设备分配）
accelerate==0.27.2

# 可视化（绘制 loss 曲线）
matplotlib==3.8.4
