# 医疗大模型 LoRA 指令微调 — 面试准备 QA 文档

> 本文档基于实际跑通的项目流程整理，涵盖项目架构、技术选型、实现细节、难点解决与深度追问。
> 答案均与真实代码、真实训练数据对齐，可直接用于面试表述。

---

## 第一部分：项目概览与架构设计

### Q1：请简要介绍你的项目

**参考答案：**

我做了一个医疗大模型的指令微调实践项目。基座模型用 Qwen1.5-0.5B，数据集用 MedicalGPT 项目整理的医疗问答数据（shibing624/medical，抽样 2000 条纯中文 SFT 数据），通过 LoRA 方式微调。

核心数据：
- 基座参数：464,774,144（约 4.6 亿）
- LoRA 可训练参数：786,432（约 78 万）
- 可训练占比：**0.1692%**，压缩 **591 倍**
- 训练 loss：从初始 2.42 下降到最低 2.05，下降约 15%
- 训练时长：约 10 分钟（RTX 2060 SUPER，本地）
- 训练完成后做了 5 个医学问题的推理对比，验证微调效果

项目架构：
```
MedicalChatGPT/
├── config.py              # 统一配置中心
├── data/prepare_data.py   # 数据下载 + 抽样 + ChatML 格式转换
├── train_lora.py          # LoRA 训练（参数量打印 + loss 回调）
├── inference_compare.py   # 推理对比（基座 vs 微调后）
├── plot_loss.py           # loss 曲线可视化
├── colab/                 # Colab 一站式笔记本
└── results/               # adapter 权重 + loss 日志 + 对比结果
```

---

### Q2：为什么选择 LoRA 而不是全量微调？

**参考答案：**

三个核心原因：

1. **显存效率**：全量微调需要存储全部参数的梯度和优化器状态（Adam 需存 momentum + variance，是参数量的 2 倍），0.5B 模型全量微调需要约 12GB 显存。LoRA 只训练 0.17% 的参数，优化器状态极小，本地 8GB 显存即可跑。

2. **防过拟合**：只有 2000 条数据，全量微调 4.6 亿参数极易过拟合。LoRA 通过低秩约束限制了可学习的方向，相当于一种正则化。

3. **工程实用**：LoRA adapter 只有几 MB，可以方便地切换、组合、分发。生产环境中多个任务可以共享一个基座 + 多个 LoRA adapter。

**追问：LoRA 效果比全量微调差多少？**

研究表明，在数据量适中（几千到几万条）的 SFT 场景下，LoRA 效果接近全量微调（差距通常在 1-3%）。但当任务与预训练数据分布差异极大，或数据量非常充足（百万级）时，全量微调会更优。我的项目是轻量级实践，LoRA 是合理选择。

---

### Q3：LoRA 的原理是什么？

**参考答案：**

LoRA 的核心公式：

```
h = W₀·x + ΔW·x
```

- `x`：输入向量（上一层的激活值）
- `W₀`：预训练权重，**冻结不训练**
- `ΔW`：增量权重，**只训练这部分**，代表"领域修正量"

关键 trick 是**低秩分解**：`ΔW = A·B`

原本 ΔW 是 `[d, d]` 的矩阵（如 1024×1024 = 100 万参数），LoRA 把它分解成两个小矩阵：
- `A`：`[d, r]`（如 1024×8 = 8192 参数）
- `B`：`[r, d]`（如 8×1024 = 8192 参数）
- 总参数：16384，比原来的 100 万减少约 64 倍

**为什么能这么分解？** 背后的假设是：领域微调引起的权重变化 ΔW 通常是低秩的（即可以用很少的"方向"来描述），所以用低秩矩阵近似不会损失太多信息。

**追问：r（秩）怎么选？**

- r 越大：表达能力越强，但参数越多，越容易过拟合
- r 越小：参数越少，但可能欠拟合
- 常用值：r=4/8/16/32，r=8 是 SFT 的常用小值配置
- 我的项目用 r=8，在 0.5B 模型 + 2000 条数据下是合理的

---

### Q4：为什么 target_modules 选 q_proj 和 v_proj？

**参考答案：**

Transformer 的注意力层有四个投影矩阵：q_proj（Query）、k_proj（Key）、v_proj（Value）、o_proj（Output）。

LoRA 原始论文的实验结论是：**只在 q_proj 和 v_proj 上注入 LoRA，就能获得接近全量注入的效果，且参数量更少**。这是因为 Q 和 V 是注意力计算中最影响"关注什么"和"提取什么"的两个矩阵。

**追问：如果效果不好可以怎么调整？**

- 扩展 target_modules 到全部线性层（q/k/v/o + MLP 的 gate/up/down）
- 增大 r（如 r=16 或 r=32）
- 这两者都会增加可训练参数，需要更多数据避免过拟合

---

## 第二部分：数据处理与格式

### Q5：数据集是怎么选择的？为什么用这个？

**参考答案：**

选了 MedicalGPT 项目（shibing624/medical）整理的医疗问答数据集，具体用的是 `finetune/train_zh_0.json` 子集（纯中文，约 194 万条），抽样 2000 条。

选择原因：
1. **数据质量有保障**：MedicalGPT 是持续维护的开源项目，数据经过清洗
2. **字段规范**：标准的三段式 `{instruction, input, output}` 格式
3. **领域匹配**：中文医疗问答，和目标场景一致

**追问：为什么只取 2000 条？**

- 实践项目优先跑通流程，2000 条足够验证 LoRA 效果
- 数据量小训练快（10-40 分钟），便于迭代
- 避免过拟合：0.5B 模型 + LoRA，数据太多反而可能学到噪声

---

### Q6：数据的 input 字段是怎么处理的？

**参考答案：**

原始数据有三段：`instruction`（指令）、`input`（补充信息，常为空）、`output`（答案）。

处理规则：
- `input` 非空时：拼成 `{instruction}\n{input}` 作为 user 内容（保留病人描述）
- `input` 为空时：直接用 `instruction`

实际数据中 input 非空的比例较低，但当它有值时通常承载病人具体描述（如年龄、症状），对医疗场景很重要，不能丢弃。

---

### Q7：ChatML 格式是什么？为什么要用？

**参考答案：**

ChatML 是 Qwen 模型约定的对话标记格式，用特殊标记 `<|im_start|>` 和 `<|im_end|>` 切分角色：

```
<|im_start|>system
你是一个专业的医疗助手。<|im_end|>
<|im_start|>user
感冒了应该怎么办？<|im_end|>
<|im_start|>assistant
多休息，多喝水...<|im_end|>
```

**为什么要用：**
1. **训练与推理格式一致**：训练时模型学到"看到 user 提问 → 生成 assistant 回答"的模式，推理时用同样格式才能正确触发
2. **角色区分**：模型通过标记知道哪句是系统设定、哪句是用户问题、哪句该自己生成
3. **system prompt 固定人设**：每条数据都带"你是一个专业的医疗助手"，强化模型的医疗助手身份

---

### Q8：什么是 Label Masking？为什么需要？

**参考答案：**

Label Masking 是指训练时对 prompt 部分（system + user）的 token 将 label 设为 -100，只在 assistant 回答部分计算 loss。

```python
# prompt 部分（system + user）→ label = -100（不计算 loss）
# assistant 部分 → label = 真实 token id（计算 loss）
labels = [-100] * len(prompt_ids) + response_ids[:]
```

**为什么需要：**
- 如果不 mask，模型会对"问题本身"也计算 loss，变成"学习背诵问题"
- mask 后，模型只学习"给定问题，如何生成回答"，这才是 SFT 的目标
- -100 是 PyTorch CrossEntropyLoss 的默认 ignore_index，遇到 -100 的位置不计算梯度

---

## 第三部分：训练配置与优化

### Q9：你的显存优化策略有哪些？

**参考答案：**

我在 RTX 2060 SUPER（8GB）上训练，用了四层优化：

| 策略 | 配置 | 作用 |
|------|------|------|
| 混合精度训练 | FP32 权重 + Trainer fp16=True | 前向计算用 FP16 省显存，权重用 FP32 保精度 |
| Gradient Checkpointing | `gradient_checkpointing=True` | 牺牲约 20% 计算时间，大幅减少激活内存 |
| 小 batch + 梯度累积 | batch=1, accum=8 | 等效 batch=8，但每次仅 1 条在显存 |
| 序列长度限制 | max_seq_len=512 | 控制单条 token 数上限 |

预估峰值显存约 3-4GB，8GB 显存绰绰有余。

---

### Q10：混合精度训练的原理是什么？你踩过什么坑？

**参考答案：**

混合精度训练的核心是"FP32 和 FP16 混着用"：
- **权重存储**：FP32（作为 master weights，保证精度）
- **前向计算**：临时转 FP16（省显存、加速）
- **梯度计算**：FP32（保证精度）
- **权重更新**：FP32（精确更新）

**GradScaler 的作用**：FP16 能表示的最小数有限，小梯度会下溢出变成 0。GradScaler 先把 loss 放大 N 倍 → 梯度也放大 → 不易变 0 → 更新前再 unscale 回来。

**我踩的坑**：

一开始我用 `torch_dtype=torch.float16` 加载模型，同时 Trainer 开了 `fp16=True`，结果报错：
```
ValueError: Attempting to unscale FP16 gradients.
```

原因：模型已经是 FP16 → LoRA 参数也是 FP16 → 梯度也是 FP16，而 GradScaler 要求梯度是 FP32 才能 unscale。

**修复**：模型改用 `torch_dtype=torch.float32` 加载，让 Trainer 自己负责混合精度（前向时自动转 FP16）。这样权重和梯度都是 FP32，GradScaler 能正常工作。

**核心教训**：`fp16=True` 是让 Trainer 负责"混合精度"，所以模型本身必须是 FP32 作为主权重。不能自己先 FP16 加载，否则等于跳过了 master weights 这层，GradScaler 的工作流程就断了。

---

### Q11：Gradient Checkpointing 的原理是什么？

**参考答案：**

正常反向传播需要保存前向每一层的激活值（用于算梯度），激活值随层数线性增长，是显存大头。

Gradient Checkpointing 的做法：
- **前向时**：不保存中间层激活值，只保存某些"检查点"层的输出
- **反向时**：需要某层激活值时，重新做一次前向计算把它算出来

**代价**：多一次前向计算，训练慢约 20%
**收益**：激活内存从 O(n) 降到 O(√n)，大幅省显存

**追问：为什么用 gradient checkpointing 要配合 enable_input_require_grads？**

因为 gradient checkpointing 会让输入不保存梯度，而 LoRA 需要梯度回传到 LoRA 层。`enable_input_require_grads()` 让输入 require_grad=True，保证梯度能从 LoRA 层回传。

---

### Q12：为什么用 batch_size=1 + 梯度累积=8？

**参考答案：**

直接用 batch_size=8 需要同时把 8 条数据放进显存，峰值显存会翻 8 倍，8GB 显存放不下。

梯度累积的原理：
```
step 1: 前向+反向 → 累积梯度（不更新权重）
step 2: 前向+反向 → 累积梯度
...
step 8: 前向+反向 → 累积梯度 → 除以 8 → 更新权重
```

**效果**：数学上等效于 batch_size=8（梯度是 8 条数据的平均），但每次只放 1 条在显存。

**追问：梯度累积和真 batch 有区别吗？**

有微小区别：
- 真 batch：8 条数据同时算，BatchNorm 等层会看到 8 条的统计量
- 梯度累积：逐条算，BatchNorm 只看到 1 条（但 Transformer 用 LayerNorm，不受影响）
- 对于 LayerNorm 的 Transformer，两者几乎完全等效

---

### Q13：你的学习率为什么用 2e-4？

**参考答案：**

LoRA 微调的常用学习率范围是 1e-4 到 5e-4，比全量微调（通常 1e-5 到 5e-5）高约 10 倍。

原因：LoRA 只训练少量参数，需要更大的学习率才能让这些参数学到足够的领域知识。如果用全量微调的学习率（1e-5），LoRA 参数更新太慢，训练不充分。

我选 2e-4 是 LoRA SFT 的中常用值，配合 cosine 学习率调度 + 3% warmup（前期小幅上升防前期不稳定，后期平滑下降）。

---

## 第四部分：训练结果分析

### Q14：你的 loss 曲线表现如何？

**参考答案：**

训练 750 步（3 epoch），loss 表现：
- 初始 loss（step 10）：2.4232
- 最低 loss（step 510）：2.0529
- 最终 loss（step 750）：2.1830
- 下降幅度：约 15%

**趋势分析**：
- step 0-150：缓慢下降，波动大（batch=1，单条数据难度差异大）
- step 150-510：明显下降，达到最低点
- step 510-750：略有回升，趋于平稳（可能轻微过拟合或数据已学完）

**追问：为什么 loss 没有降到很低（如 1.0 以下）？**

四个原因：
1. **模型容量小**：Qwen1.5-0.5B 只有 4.6 亿参数，学习能力有限
2. **LoRA 只训练 0.17% 参数**：基座权重完全冻结，调整空间有限
3. **数据量少**：2000 条 × 3 epoch，数据量不足
4. **医疗回答的固有不确定性**：同一个问题可以有多种合理回答，loss 不会收敛到 0

这个结果符合轻量级实践的预期，重点验证了流程跑通和 LoRA 参数骤减效果。

---

### Q15：loss 有波动正常吗？

**参考答案：**

正常。波动主要来自 `batch_size=1`：每步只看 1 条数据，有些问题简单（如"感冒怎么办"），有些很专业（如"进展性脑卒的辅助治疗"），loss 自然跳动。

如果看移动平均线，下降趋势清晰。如果要减少波动，可以：
- 增大 batch_size（但显存压力增大）
- 用梯度累积等效增大 batch（我已经用了 accum=8，但 logging 是按 step 记录的，所以单条波动仍可见）

---

### Q16：如何判断模型是否过拟合？

**参考答案：**

观察 loss 曲线的趋势：
- **正常**：loss 持续下降或平稳
- **过拟合信号**：train loss 继续下降，但 val loss 开始上升

我的项目中 step 510 后 loss 略有回升（2.05 → 2.18），可能是轻微过拟合或数据已学完。但因为只有 train loss（没设 validation set），无法严格判断。

**更严谨的做法**：
- 划分 10% 数据作为 validation set
- 训练时同时记录 train_loss 和 val_loss
- 如果 val_loss 上升而 train_loss 下降，确认过拟合，应早停

**追问：你的项目为什么没设 validation set？**

轻量级实践优先跑通流程，2000 条数据再切分验证集会让训练数据更少。如果要做严谨实验，应该切分并监控 val_loss。

---

## 第五部分：推理与部署

### Q17：推理时怎么加载微调后的模型？

**参考答案：**

LoRA 的优势之一就是推理时灵活加载：

```python
# 1. 加载基座模型（FP16，推理不需要 FP32）
base_model = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen1.5-0.5B",
    torch_dtype=torch.float16,
    device_map="auto",
)

# 2. 在基座上叠加 LoRA adapter
tuned_model = PeftModel.from_pretrained(base_model, "./results/lora_adapter")
```

`PeftModel.from_pretrained` 会把训练好的 A、B 矩阵加载到基座对应的 q_proj/v_proj 层上，推理时自动计算 `h = W₀·x + (A·B)·x`。

**追问：推理时可以用 FP16 吗？**

可以。推理不需要更新权重，不需要 GradScaler，直接用 FP16 省显存、加速。这和训练时必须用 FP32 主权重是两回事。

---

### Q18：推理时的 prompt 格式和训练时一样吗？

**参考答案：**

必须一样。推理时构造的 prompt：

```
<|im_start|>system
你是一个专业的医疗助手。<|im_end|>
<|im_start|>user
感冒了应该怎么办？<|im_end|>
<|im_start|>assistant
```

和训练数据的格式完全一致（system + user），只是少了 assistant 的回答部分。模型看到这个格式后，会从 `<|im_start|>assistant` 后开始生成回答。

**如果格式不一致会怎样**：模型可能无法正确理解输入，生成质量下降，甚至输出乱码。这是 SFT 的基本要求——训练和推理格式必须对齐。

---

### Q19：生成参数（temperature、top_p）是什么意思？

**参考答案：**

- **temperature=0.7**：控制生成的随机性。值越高越随机多样，越低越确定保守。0.7 是平衡多样性和合理性的常用值。
- **top_p=0.9**：核采样（nucleus sampling），只从概率累积达到 90% 的 token 中采样，过滤掉低概率的噪音 token。
- **do_sample=True**：启用采样模式（而非贪心解码），配合 temperature 和 top_p 使用。

**追问：什么时候用贪心解码（do_sample=False）？**

需要确定性输出的场景，如医疗诊断建议、代码生成。贪心解码每步选概率最高的 token，输出固定。但容易重复啰嗦，对话场景通常用采样。

---

## 第六部分：深度追问

### Q20：如果显存还不够怎么办？（QLoRA）

**参考答案：**

如果 8GB 显存仍不够，可以用 QLoRA（Quantized LoRA）：
- 把基座模型权重量化到 4-bit（用 bitsandbytes），显存占用减少约 75%
- 0.5B 模型从 2GB → 0.5GB
- LoRA 参数仍用 FP16/BF16 训练，保证精度

QLoRA 和 LoRA 的区别：
- LoRA：基座 FP16/FP32 + LoRA 参数训练
- QLoRA：基座 4-bit 量化 + LoRA 参数训练（额外需要 nf4 量化 + 双重量化 + paged optimizer）

**代价**：4-bit 量化会有轻微精度损失，训练速度略慢（反量化开销）。

---

### Q21：如果效果不好，你会怎么优化？

**参考答案：**

按优先级排查：

1. **数据层面**（通常最重要）
   - 增大数据量（2000 → 10000+）
   - 清洗数据质量（去除"常规治疗"这种无信息回答）
   - 检查 input/output 字段是否对齐

2. **模型层面**
   - 换更大的基座（Qwen1.5-1.8B 或 Qwen2.5-1.5B）
   - 扩展 target_modules（加 k_proj、o_proj、MLP 层）
   - 增大 r（8 → 16 或 32）

3. **训练层面**
   - 增加 epoch（3 → 5），但要监控过拟合
   - 调整学习率（2e-4 → 1e-4 或 5e-4）
   - 增大 batch_size（如果显存允许）

4. **评估层面**
   - 划分 validation set，监控 val_loss
   - 用更多测试问题评估，不只看 5 个
   - 考虑用 BLEU/ROUGE 等指标量化，不只靠人工看

---

### Q22：你的项目有哪些局限性？

**参考答案：**

1. **模型太小**：0.5B 参数，医疗知识容量有限，无法替代专业模型
2. **数据量少**：2000 条，不足以充分学习医疗领域知识
3. **无量化评估**：只有人工对比 5 个问题，没有 BLEU/ROUGE 等客观指标
4. **无 validation set**：无法严格判断过拟合
5. **单轮问答**：只支持单轮，不支持多轮对话（需改为多轮数据格式）
6. **无安全性保障**：医疗建议可能不准确，不能用于实际医疗场景

**这是一个实践项目，目标是跑通流程、理解原理，不是追求生产可用效果。**

---

### Q23：全量微调、LoRA、Prefix Tuning 的区别？

**参考答案：**

| 方法 | 训练参数 | 显存 | 效果 | 适用场景 |
|------|---------|------|------|---------|
| 全量微调 | 100% | 最高 | 最优 | 数据充足、资源充足 |
| LoRA | 0.1%-1% | 低 | 接近全量 | SFT 主流方法 |
| Prefix Tuning | <0.1% | 最低 | 略逊 | 极度显存受限 |
| QLoRA | 0.1%-1% | 很低 | 接近 LoRA | 大模型 + 小显存 |

**为什么 LoRA 成为主流**：
- 效果接近全量微调
- 参数量小，可方便切换/组合多个 adapter
- 训练稳定，不容易过拟合
- 推理时可以把 LoRA 权重 merge 回基座，无额外开销

---

### Q24：LoRA 推理时可以 merge 回基座吗？

**参考答案：**

可以。LoRA 训练后可以把 ΔW = A·B 直接加到 W₀ 上：

```python
merged_model = tuned_model.merge_and_unload()
```

merge 后的模型和原基座结构完全一样，只是权重变了。推理时没有任何额外开销（不需要额外算 A·B）。

**什么时候 merge**：
- 单任务部署：merge 后推理更快
- 多任务切换：不 merge，动态加载不同 adapter（更灵活）

---

### Q25：你在这个项目中学到了什么？

**参考答案：**

**技术层面**：
1. 理解了 LoRA 的数学原理（低秩分解 ΔW = A·B）和工程实现（PEFT 库的使用）
2. 掌握了显存优化的多种手段（混合精度、gradient checkpointing、梯度累积）
3. 理解了 SFT 的数据流程（ChatML 格式、label masking）
4. 踩了混合精度的坑（FP16 加载 + fp16=True 冲突），深入理解了 GradScaler 的工作机制

**工程层面**：
1. 学会了配置中心化设计（config.py 统一管理参数）
2. 理解了训练监控的重要性（loss 回调实时记录）
3. 体验了双环境交付（本地 .py + Colab .ipynb）

**认知层面**：
1. 认识到数据质量比数据量更重要（"常规治疗"这种回答无信息量）
2. 理解了轻量级实践的价值：跑通流程比追求效果更重要
3. 学会了用实际数据验证理论（参数量 0.17% 验证了 LoRA 的参数骤减效果）

---

## 附录：一句话总结（面试开场用）

> 我做过一个医疗大模型的 LoRA 指令微调实践。用 Qwen1.5-0.5B 作为基座，用 MedicalGPT 项目整理的医疗问答数据（2000 条），通过 LoRA 微调，可训练参数只有 78 万，占总参数的 0.17%，压缩了 591 倍。在 RTX 2060 上约 10 分钟训练完成，loss 从 2.42 降到 2.05。过程中我用混合精度、gradient checkpointing、梯度累积等策略把显存控制在 4GB 以内，并解决了 FP16 加载与混合精度训练冲突的坑。整个项目让我深入理解了 LoRA 原理、SFT 数据流程和显存优化策略。
